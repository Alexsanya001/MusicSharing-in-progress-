storage:
  provider: aws

  minio:
    endpoint: https://localhost:9000
    region: us-east-1
    access-key: ${MINIO_ADMIN}
    secret-key: ${MINIO_PASSWORD}
    bucket-name: music

  aws:
    region: eu-north-1
    access-key: ${AWS_ACCESS_KEY}
    secret-key: ${AWS_SECRET_KEY}
    bucket-name: alexsmolikin1music